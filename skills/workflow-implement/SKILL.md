---
name: workflow-implement
description: 按任务列表执行代码实现。读取 tasks.md，按拓扑顺序执行任务，支持 TDD 模式、并行执行和原子提交。内置质量检查和断点恢复。当用户想要"执行任务"、"开始实现"、"写代码"时使用。也响应 "workflow implement"、"工作流实现"。
---

# Workflow Implement

按任务列表执行代码实现：LOAD → PLAN → EXECUTE → REVIEW → COMMIT → REPORT

---

## 🚀 执行流程

**当此 skill 被触发时，你必须按以下流程执行：**

### 立即行动

1. 验证 `.workflow/{feature}/task/tasks.md` 存在且 status: approved
2. **询问用户审查方式**：使用 AskUserQuestion 让用户选择:
   - **选项 1: Codex 审查**（推荐）- 使用 /codex skill 进行高质量审查
   - **选项 2: 独立 Agent 审查** - 使用 Task 工具启动独立审查 Agent
3. 记录审查方式到 `.state.yaml`
4. 创建目录: `.workflow/{feature}/implement/`
5. 开始 Phase 1: LOAD

### 📋 进度追踪 Checklist

**复制此清单并逐项完成：**

```
- [ ] Phase 1: LOAD → 构建任务 DAG
- [ ] Phase 2: PLAN → 生成批次执行计划
- [ ] Phase 3: EXECUTE → 执行所有任务
- [ ] Phase 4: REVIEW → 判定: PASS 进入下一阶段
- [ ] Phase 5: COMMIT → 输出: commits/commit-log.md
- [ ] Phase 6: REPORT → 输出: implement-report.md
```

### ✅ 阶段完成验证

| 阶段 | 完成条件 | 下一步 |
|------|----------|--------|
| LOAD | 任务 DAG 已构建 | → PLAN |
| PLAN | 批次划分完成 | → EXECUTE |
| EXECUTE | 所有任务已执行 | → REVIEW |
| REVIEW | 判定 PASS | → COMMIT |
| COMMIT | 提交记录已生成 | → REPORT |
| REPORT | `implement-report.md` 存在 | → 结束 |

---

## Phase 详情

### Phase 1: LOAD（加载任务）

**你必须：**
1. 验证 tasks.md 存在且 status: approved
2. 解析任务列表，提取: ID、标题、优先级、标记、依赖
3. 构建 DAG（有向无环图）
4. 验证 DAG 无循环依赖
5. 初始化 `.state.yaml`

**错误处理**:
- tasks.md 不存在 → 中止，提示路径
- 状态非 approved → 中止，提示状态
- 循环依赖 → 中止，显示循环

**完成标志**: 任务 DAG 已构建

---

### Phase 2: PLAN（执行计划）

**你必须：**
1. 拓扑排序确定执行顺序
2. 识别并行批次（同批次任务无依赖）
3. 计算关键路径
4. 生成执行计划

**批次划分**:
```
批次 1: [无依赖的任务]
批次 2: [仅依赖批次1的任务]
批次 N: [仅依赖批次1..N-1的任务]
```

**完成标志**: 批次划分完成

---

### Phase 3: EXECUTE（任务执行）

**你必须：**
1. 按批次顺序执行任务
2. 对 [P] 标记任务使用 Task 工具并行执行
3. 对 [T] 标记任务执行 TDD 流程:
   - 编写测试 → 验证红灯 → 编写实现 → 验证绿灯 → 重构
4. 更新 .state.yaml 中的任务状态
5. 记录执行日志到 `logs/batch-{N}.log`

**失败处理**:
- P0/P1 任务失败 → 立即中止
- P2/P3 任务失败 → 跳过继续

**完成标志**: 所有任务已执行（完成或跳过）

---

### Phase 4: REVIEW（质量检查）

**你必须：**
1. 根据用户在"立即行动"阶段选择的审查方式执行:
   - **Codex 审查**: 使用 Skill 工具调用 `/codex` skill，传递实现的代码变更
   - **独立 Agent 审查**: 使用 Task 工具启动独立审查 Agent
2. 运行项目测试
3. 运行 lint 检查
4. 统计任务完成率

**判定规则**:
- **PASS**: 测试 100% 通过，lint 错误 0 → COMMIT
- **NEEDS_FIX**: 部分不通过 → 回退 EXECUTE（最多 3 轮）
- **REJECTED**: 关键任务失败 或 超过 3 轮 → 中止

**完成标志**: 判定为 PASS

---

### Phase 5: COMMIT（原子提交）

**你必须：**
1. 检测每个任务关联的文件变更
2. 为每个任务创建 git commit
3. 使用格式: `task({task-id}): {task-title}`
4. 记录 commit SHA 到 `commits/commit-log.md`

> 模板: [assets/commit-template.md](assets/commit-template.md)

**完成标志**: `commits/commit-log.md` 存在

---

### Phase 6: REPORT（生成报告）

**你必须：**
1. 汇总执行摘要（总数、完成、跳过、失败）
2. 生成任务详情表
3. 记录审查结果
4. 创建 `implement-report.md`

**完成标志**: `implement-report.md` 存在

---

## 目录结构

```
.workflow/{feature}/implement/
├── .state.yaml
├── logs/
│   └── batch-{N}.log
├── commits/
│   └── commit-log.md
├── reviews/
│   └── round-{N}/review.md
└── implement-report.md
```

---

## 任务标记说明

| 标记 | 含义 | 执行策略 |
|------|------|----------|
| `[T]` | TDD | 先写测试，再实现 |
| `[P]` | 可并行 | 使用 Task 工具并行执行 |
| `[R]` | 需审查 | 完成后需人工审查 |

## 任务状态

| 状态 | 标记 | 说明 |
|------|------|------|
| 待执行 | `[ ]` | 尚未开始 |
| 进行中 | `[~]` | 正在执行 |
| 已完成 | `[x]` | 成功完成 |
| 失败 | `[!]` | 执行失败 |
| 跳过 | `[-]` | 被跳过 |

---

## 资源

| 资源 | 路径 | 用途 |
|------|------|------|
| 状态模板 | [assets/state-template.yaml](assets/state-template.yaml) | LOAD 阶段 |
| 提交模板 | [assets/commit-template.md](assets/commit-template.md) | COMMIT 阶段 |
| 阶段详情 | [references/phase-details.md](references/phase-details.md) | 详细子任务 |

---

## 集成

**输入**: `/workflow-task` 生成的 `tasks.md`（已批准）
**输出**: 代码变更 + 执行报告，供 `/workflow-review` 使用
